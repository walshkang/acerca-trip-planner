'use client'

/**
 * @deprecated Use `WorkspaceContainer` from `@/components/workspace/WorkspaceContainer`.
 * Kept as a compatibility alias for imports that still reference the old name.
 */
export { default } from '@/components/workspace/WorkspaceContainer'
