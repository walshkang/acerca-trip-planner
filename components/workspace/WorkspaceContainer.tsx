'use client'

/**
 * @deprecated Use `ExploreShell` from `@/components/app/ExploreShell`.
 * Kept as a compatibility alias during the Two-Journey Architecture migration.
 */
export { default } from '@/components/app/ExploreShell'
